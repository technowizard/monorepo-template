# assets

Place static assets here:

- `images/` - any raster images or SVGs used as img src
- `icons/` - app icons, favicon
- `fonts/` - self-hosted font files (woff2 preferred)

For component icons, use @phosphor-icons/react instead of asset files
